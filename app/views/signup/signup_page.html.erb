<!-->
<!DOCTYPE html>
<html >
<head>
  <meta charset="UTF-8">
  <title>Sign Up</title>
    <%= stylesheet_link_tag "signup.scss" %>
    <div class="flex">
        <h1>Sign Up Here!</h1>
      </flex>
    </div>
</head>

<body>
  <body class="bg">
  <div class="registeration">
  <div class="register-form">
        <input type="text" placeholder="username"/>
        <input type="password" placeholder="password"/>
        <input type="text" placeholder="email address"/>
        <%= link_to 'CREATE', "/home/home_page" , :style=>"color: white;", class:"button" %>
        <p class="message">Already Registered? <%= link_to 'Login in Here', "../" %></p>
      </div>
    </register-form>
  </registeration>
</bg>
</body>
</html>
<-->

<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :emailss %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <%= f.text_field :password %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
